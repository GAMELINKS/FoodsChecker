<% if user_signed_in? %>
<p id="notice"><%= notice %></p>

<%= render 'search_form' %>

<%= render 'foods_index' %>

<% else %>
  <p><%= link_to "Mastodonアカウントでログイン", user_mastodon_omniauth_authorize_path %></p>
<% end %>
